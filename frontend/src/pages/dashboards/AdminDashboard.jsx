import BaseRoleDashboard from "./BaseRoleDashboard";

export default function AdminDashboard() {
  return <BaseRoleDashboard endpoint="admin" title="Admin Control Tower" subtitle="Platform operations, governance posture, and administrative controls." />;
}
